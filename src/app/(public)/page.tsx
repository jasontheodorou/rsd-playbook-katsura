export const dynamic = 'force-dynamic'

/** Placeholder until the read model exists (build step 2). Renders with no JavaScript and no database. */
export default function HomePage() {
  return (
    <article>
      <h1>katsura</h1>
      <p>Nothing has been published yet.</p>
    </article>
  )
}
